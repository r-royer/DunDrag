@model Personnage

@{
    ViewData["Title"] = "Index";
}

<h2>Liste des sorts de @Model.Nom</h2>
   
<div class="row">
    @foreach (PersonnageSort personnageSort in Model.PersonnagesSorts)
    {
        @await Html.PartialAsync("_DetailsSortMinPartial", personnageSort.Sort)
    }
</div>